# GENERATED by applications/dynacell/tools/generate_spotlight_v2_leaves.py -- edit the
# generator, not this file (plan: ~/.claude/plans/spotlight-v2.md).
# Spotlight-v2 `segaux` arm (fit) of the baseline leaf membrane/pix2pix3d_unetvit/ipsc_confocal/train_4gpu_modernized.yml.
# Recipe delta vs that leaf: data fg_mask_key: fg_mask; model seg_aux (SegAuxDice c=0.1) + seg_aux_weight (+ seg_aux_t0 for flow).
# Everything else, and the rationale for it, is the baseline leaf's.
base:
- ../../../_internal/shared/model/train_sets/ipsc_confocal.yml
- ../../../_internal/shared/model/targets/membrane.yml
- ../../../_internal/shared/model/data_overlays/unetvit3d_fit.yml
- ../../../_internal/shared/model/model_overlays/pix2pix3d_unetvit_fit_ddp_modernized.yml
- ../../../_internal/shared/model/launcher_profiles/mode_fit.yml
- ../../../_internal/shared/model/launcher_profiles/hardware_4gpu.yml
- ../../../_internal/shared/model/launcher_profiles/runtime_shared.yml
benchmark:
  task: virtual_staining
  organelle: membrane
  train_set: ipsc_confocal
  model_name: pix2pix3d_unetvit_segaux
  experiment_id: membrane__ipsc_confocal__pix2pix3d_unetvit_segaux
model:
  init_args:
    lambda_l1: 10.0
    lecam_gamma: 0.3
    seg_aux:
      class_path: viscy_utils.losses.SegAuxDice
      init_args:
        c: 0.1
    # calibrated value pending: 1.0 is a PLACEHOLDER. Set it per family from the
    # gradient-norm calibration (plan: 'w calibration') before submitting this fit.
    seg_aux_weight: 1.0
trainer:
  max_epochs: 40
  logger:
    init_args:
      name: pix2pix3d_unetvit_iPSC_MEMB_4gpu_modernized_lambdaL1_10_lecam_40ep_segaux
      save_dir: /hpc/projects/comp.micro/virtual_staining/models/dynacell/ipsc/membrane/pix2pix3d_unetvit_segaux
  callbacks:
  - class_path: lightning.pytorch.callbacks.LearningRateMonitor
    init_args:
      logging_interval: step
  - class_path: lightning.pytorch.callbacks.ModelCheckpoint
    init_args:
      monitor: loss/validate_ema
      filename: epoch={epoch}-step={step}-loss={loss/validate_ema:.3f}
      auto_insert_metric_name: false
      every_n_epochs: 1
      save_top_k: 4
      save_last: true
      dirpath: /hpc/projects/comp.micro/virtual_staining/models/dynacell/ipsc/membrane/pix2pix3d_unetvit_segaux/checkpoints
launcher:
  job_name: pix2pix3d_unetvit_iPSC_MEMB_4gpu_modernized_lambdaL1_10_lecam_40ep_segaux
  run_root: /hpc/projects/comp.micro/virtual_staining/models/dynacell/ipsc/membrane/pix2pix3d_unetvit_segaux
  sbatch:
    constraint: h200
data:
  init_args:
    fg_mask_key: fg_mask
