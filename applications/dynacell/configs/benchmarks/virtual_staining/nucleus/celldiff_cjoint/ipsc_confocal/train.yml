# GENERATED by applications/dynacell/tools/generate_spotlight_v2_leaves.py -- edit the
# generator, not this file (plan: ~/.claude/plans/spotlight-v2.md).
# Spotlight-v2 `cjoint` arm (fit) of the baseline leaf nucleus/celldiff/ipsc_confocal/train.yml.
# Recipe delta vs that leaf: data fg_mask_key: fg_mask; model net_config.in_channels 2, mask_mode joint, mask_dice_weight, mask_velocity_weight 1.0, seg_aux_t0 0.7.
# Everything else, and the rationale for it, is the baseline leaf's.
base:
- ../../../_internal/shared/model/train_sets/ipsc_confocal.yml
- ../../../_internal/shared/model/targets/nucleus_celldiff.yml
- ../../../_internal/shared/model/data_overlays/celldiff_fit.yml
- ../../../_internal/shared/model/model_overlays/celldiff_fit.yml
- ../../../_internal/shared/model/launcher_profiles/mode_fit.yml
- ../../../_internal/shared/model/launcher_profiles/hardware_h200_single.yml
- ../../../_internal/shared/model/launcher_profiles/runtime_shared.yml
benchmark:
  task: virtual_staining
  organelle: nucleus
  train_set: ipsc_confocal
  model_name: celldiff_cjoint
  experiment_id: nucleus__ipsc_confocal__celldiff_cjoint
trainer:
  logger:
    init_args:
      name: CELLDiff_iPSC_NUCL_cjoint
      save_dir: /hpc/projects/comp.micro/virtual_staining/models/dynacell/ipsc/nucleus/celldiff_cjoint
  callbacks:
  - class_path: lightning.pytorch.callbacks.LearningRateMonitor
    init_args:
      logging_interval: step
  - class_path: lightning.pytorch.callbacks.ModelCheckpoint
    init_args:
      every_n_epochs: 1
      save_top_k: -1
      save_last: true
      dirpath: /hpc/projects/comp.micro/virtual_staining/models/dynacell/ipsc/nucleus/celldiff_cjoint/checkpoints
launcher:
  job_name: CELLDiff_NUCL_cjoint
  run_root: /hpc/projects/comp.micro/virtual_staining/models/dynacell/ipsc/nucleus/celldiff_cjoint
data:
  init_args:
    fg_mask_key: fg_mask
model:
  init_args:
    net_config:
      in_channels: 2
    mask_mode: joint
    # calibrated value pending: 1.0 is a PLACEHOLDER. Set it per family from the
    # gradient-norm calibration (plan: 'w calibration') before submitting this fit.
    mask_dice_weight: 1.0
    mask_velocity_weight: 1.0
    seg_aux_t0: 0.7
