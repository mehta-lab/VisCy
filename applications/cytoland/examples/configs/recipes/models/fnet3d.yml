# Model recipe: FNet3D — recursive encoder-decoder (Ounkomol et al. 2018).
model:
  class_path: cytoland.engine.VSUNet
  init_args:
    architecture: FNet3D
    model_config:
      in_channels: 1
      out_channels: 1
      depth: 4
      mult_chan: 32
      in_stack_depth: 32
