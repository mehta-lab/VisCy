# viscy-models

Neural network architectures for virtual staining microscopy.

This package provides the model definitions (UNet, contrastive, VAE) used
by the VisCy project. It is part of the VisCy workspace and designed to be
installed independently of training or data-loading infrastructure.
